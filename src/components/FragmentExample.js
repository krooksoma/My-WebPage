function FragmentExample() {
  return (
    <>
      <h1>Hello React</h1>;
    </>
  );

  /* if key is needed on the element you need to apply the following
      <React.Fragment key={1234}>
      [...]
      </React.Fragment>
  */
}

export default FragmentExample;
